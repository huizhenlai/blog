# Blog Workflow

本文件约束 Codex 在 `D:\my_web\blog` 中处理博客相关请求时的默认行为。

## 目标

当用户说“帮我写一篇博客”“把这个项目整理成文章”“先写个初稿”时，默认不是只给讨论提纲，而是直接为该博客仓库生成一篇可继续修改的草稿。

## 默认动作

如果用户明确表达“帮我写”，按下面流程执行：

1. 默认在仓库内创建或更新文章文件，而不是只在聊天里输出正文
2. 默认生成“初稿”，不是终稿
3. 默认将文章标记为 `draft: true`
4. 默认本地构建一次，确认 Hugo 没被写坏

如果用户只是想讨论选题、结构或标题，再停留在讨论层。

## 信息不足时的处理

优先少问，先做。

只有在缺少核心信息时才追问，而且最多问 1 个关键问题，例如：

- 这篇文章主要想写项目复盘、方法总结，还是论文阅读？
- 你希望这篇文章偏工程实践，还是偏原理拆解？

如果用户没有补充，允许基于现有上下文做合理假设，但必须在文中或回复中显式说明假设。

## 文章落盘规则

默认使用 page bundle 形式：

```text
content/posts/<slug>/index.md
```

规则：

- `slug` 尽量短、稳定，优先使用英文或拼音短语
- 如果文章需要图片、表格附件、截图，就和 `index.md` 放在同一目录
- 不要默认把新文章写成根目录单文件 `.md`，除非用户明确要求

## front matter 规则

创建新文章时，优先遵循：

- 模板来源：[archetypes/default.md](/D:/my_web/blog/archetypes/default.md)
- 参考示例：[template.md](/D:/my_web/blog/content/posts/template.md)

默认字段策略：

- `title`: 根据主题给出清晰标题，不追求花哨
- `summary`: 写 1 句能放到首页列表的摘要
- `date`: 使用当前时间
- `draft`: 默认 `true`
- `tags`: 默认 2 到 4 个，避免乱打
- `categories`: 默认优先复用已有分类
- `series`: 如果属于已有系列，优先复用；不强行新建系列
- `featured`: 默认 `false`，除非用户明确说要首页精选
- `math`: 只有出现公式时才设为 `true`
- `showtoc`: 默认 `true`
- `hideTitle`: 默认 `false`

## 正文写作规则

正文默认写成“可发布的初稿”，不是零散笔记。

默认结构：

```md
## 背景
## 核心内容
## 实践记录
## 结论
## 参考
```

要求：

- 不要在正文里再写 `# 一级标题`
- 直接从 `##` 开始写，避免双标题
- 内容不足时，不要硬凑完整结论，可以明确写“这里先记录当前判断”
- 对论文、前沿方法、实验结果，不能编造
- 如果某个结论只是经验判断，不要装成确定事实
- 优先写“为什么这么做、踩了什么坑、为什么没选别的方案”

## 文章类型处理

### 项目复盘

重点写：

- 背景和目标
- 实际约束
- 方案选择
- 实验或落地结果
- 遇到的问题和取舍

### 方法总结

重点写：

- 适用场景
- 方法核心假设
- 优势和局限
- 容易误用的地方

### 论文阅读

重点写：

- 论文解决什么问题
- 方法核心贡献
- 依赖什么前提
- 对实际工程有没有用

如果论文内容不完整或证据不足，允许只写“阅读笔记初稿”，不能假装全面综述。

## 系列、标签、分类维护规则

优先复用仓库已有命名，减少漂移。

当前已存在的系列包括：

- `雷达与视觉融合`
- `标定与坐标系`
- `视觉几何与测速`
- `目标检测工程实践`
- `博客建设`
- `雷达感知笔记`
- `工具与工作流`

规则：

- 同一主题的连续文章优先进入同一个 `series`
- `tags` 用细粒度关键词
- `categories` 用宽泛栏目
- 如果只是同义改写，不要新建一个几乎重复的系列名

注意：

- 仓库里已有个别标签拼写不统一，例如 `Mmware Radar`、`Object Detetion`
- 默认先复用现状，避免一次写稿时顺手引发全站 taxonomy 震荡
- 如果用户明确要清洗标签体系，再单独做一次整理

## “帮我写”时的具体执行流

当用户说“帮我写一篇关于 XXX 的博客”时：

1. 判断文章类型：项目复盘 / 方法总结 / 论文阅读 / 工具记录
2. 决定是新建文章还是续写现有文章
3. 生成 front matter
4. 写出最小可用初稿
5. 如果适合，挂到已有 `series`
6. 默认不设 `featured: true`
7. 写完后本地运行构建检查
8. 回复用户时说明：
   - 文件路径
   - 文章是初稿还是较完整版本
   - 哪些地方还需要用户补信息

## 什么时候不要直接落文件

以下情况不要直接新建文章，先确认或先讨论：

- 用户在比较多个选题，还没决定写哪篇
- 用户只是在问“这个主题值不值得写”
- 用户给的信息过少，连文章主题都无法判断
- 当前仓库已经有高度重复的同主题文章，需要先决定是续写还是合并

## 写完后的最低校验

写稿后至少做一次：

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File .\run_build.ps1
```

如果构建失败：

- 优先修 front matter
- 再检查 Markdown 语法
- 不把坏掉的草稿直接留给用户

## 回复风格

对用户汇报时保持简洁直接：

- 先说文章已经写到哪里
- 再说文件在哪
- 最后指出还缺什么信息

不要把回复写成冗长的流程说明，除非用户主动追问。
